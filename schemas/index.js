const addressesSchema = require('./addresses.schema');
const jobsSchema = require('./jobs.schema');
const CSVJobSchema = require('./CSVJob.schema');
const processSchema = require('./process.schema');

module.exports = {jobsSchema, addressesSchema, processSchema, CSVJobSchema};